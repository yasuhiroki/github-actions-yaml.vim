on: push
name: test
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: env
        run: echo TODO: soruce test/source.vim in vim by cli
